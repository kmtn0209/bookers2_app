<main>
  <p id="notice"></p>
  <div class="container">
    <div class="row">
      <div class="col-sm-12 col-md-8 col-lg-5 px-5px-sm-0 mx-auto">
        <h2>User info</h2>
        <h1>編集画面</h1>

         <%= form_with model: @user do |f| %>

          <h4>Name</h4>
          <%= f.text_field :name %>

           <h4>画像</h4>
          <%= f.file_field :image %>

          <h4>intriduction</h4>


          <%= f.submit '保存' %>

          <% end %>

      </div>
    </div>
  </div>
</main>